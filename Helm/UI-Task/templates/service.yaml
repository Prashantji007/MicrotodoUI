apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-todo-ui
  namespace: app
spec:
  type: ClusterIP
  selector:
    app: {{.Release.Name}}-todo-ui
  ports:
  - port: 80
    targetPort: 80
